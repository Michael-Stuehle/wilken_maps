using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Sitzplatz : MonoBehaviour
{
    public string Mitarbeiter;

    public string RaumName
    {
        get
        {
            return "";// gameObject.GetComponentInParent<Transform>().gameObject.GetComponentInChildren<openableDoor>().raumName;
        }
    }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
